# Assignment 2 -- Python environment
# Install into the course venv:   pip install -r requirements.txt
numpy>=2.0,<3
scipy>=1.11
matplotlib>=3.8
gdown>=5.1
opensim>=4.5      # pip wheel works (opensim==4.6); else: conda install opensim-org::opensim
