# This (mostly empty) file marks the "lib" folder as a Python package, so that
# `from lib import calibration` works from the scripts one level up.
"""Shared helpers for the IK Learning Module solution."""
